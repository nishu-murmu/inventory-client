import { Box } from '@chakra-ui/react';

//files
import Purchase from '../components/purchase';
const PurchasePage = () => {
  return (
    <Box>
      <Purchase />
    </Box>
  );
};

export default PurchasePage;
